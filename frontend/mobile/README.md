# Mobile App

This directory contains the Flutter application starter.

From this directory, run:

```bash
flutter pub get
flutter run
```

The Android/iOS platform folders are intentionally not committed yet. Generate them with:

```bash
flutter create .
```

Then run the application.

The next milestone will replace the starter home page with login, authentication state, and the student dashboard.
